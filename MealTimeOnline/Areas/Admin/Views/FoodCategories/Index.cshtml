@model IEnumerable<MealTimeOnline.Models.Commodity.FoodCategory>

@{
    ViewBag.Title = "Index";
    ViewBag.PageTitle = "Food Category";
}

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table table-striped table-hover table-bordered" id="bs-admin-foodcategory">
    <thead>
        <tr>
            <th>#</th>
            <th>
                @Html.DisplayNameFor(model => model.Value)
            </th>
            <th></th>
        </tr>
    </thead>

    <tbody>
        @{ int cnt = 0; }
        @foreach (var item in Model)
        {
            <tr>
                <td>@(++cnt)</td>
                <td>
                    @Html.DisplayFor(modelItem => item.Value)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id = item.FoodCategoryId }) |
                    @Html.ActionLink("Details", "Details", new { id = item.FoodCategoryId }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.FoodCategoryId })
                </td>
            </tr>
        }
    </tbody>
</table>

@section Styles {
    @Styles.Render("~/Content/DataTables")
}

@section Scripts {
    @Scripts.Render("~/bundles/DataTables")
    <script>
        $('#bs-admin-foodcategory').dataTable();
    </script>
}
